defaultNamespace: redis
helm:
  repo: https://charts.bitnami.com/bitnami
  chart: redis
  releaseName: redis
  values:
    architecture: standalone
    auth:
      enabled: false
targets:
  - clusterName: c-rxv24
